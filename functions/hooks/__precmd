builtin eval $SETUP

__time-cmds
if (( CMDRAN )) __reset
 
eval -- $PROMPT_COMMAND
builtin eval $SETUP
while [[ ! -d ${:P} ]] cd .. 2>/dev/null
if [[ $PWD != ${:P} && -d ${:P} ]] cd ${:P}

__prompt-init

# Debug
if (( ZSH_DEBUG )) {
  zmodload zsh/zprof
  zprof
}
