builtin eval $SETUP

__time-cmds
if (( __cmdran )) __reset
 
eval -- $PROMPT_COMMAND
builtin eval $SETUP
while [[ ! -e ${:P} ]] cd ..
if [[ $PWD != ${:P} ]] cd ${:P}

__prompt-init

# Debug
if (( ZSH_DEBUG )) {
  zmodload zsh/zprof
  zprof
}
